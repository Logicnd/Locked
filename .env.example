# Server Configuration
PORT=3000
NODE_ENV=development

# Session Secret (generate a strong random string)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Data Storage
DATA_FILE=./users.json

# Optional: Database (if switching from JSON file to MongoDB/PostgreSQL)
# DATABASE_URL=mongodb+srv://user:pass@cluster.mongodb.net/locked
# DATABASE_URL=postgresql://user:pass@localhost:5432/locked
